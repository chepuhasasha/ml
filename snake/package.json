{
  "name": "snake",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "client:build": "npm run build --workspace=@snake/client",
    "client:dev": "npm run dev --workspace=@snake/client",
    "server:build": "npm run build --workspace=@snake/server",
    "server:dev": "npm run dev --workspace=@snake/server",
    "server:start": "npm run start --workspace=@snake/server",
    "server:train": "npm run train --workspace=@snake/server",
    "server:dev:train": "npm run dev:train --workspace=@snake/server"
  },
  "author": "Sergei Batukov",
  "license": "ISC",
  "workspaces": [
    "client",
    "server"
  ]
}
